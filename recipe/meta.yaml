{% set name = "unsloth" %}
{% set version = "2026.9.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/unslothai/unsloth/archive/d90c0320664ef9812a1fb1d2a31f0977feaf7916.tar.gz
  sha256: 2d3b4abf99c84e2a3ac5e14f006801d0e76ce0a3289cae3ead51a015330a10b1

build:
  noarch: python
  script:
    - sed -i.bak "s/protobuf<4.0.0/protobuf/g" pyproject.toml
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >={{ python_min }}
    # direct
    - typer >=0.12
    - rich
    - pydantic
    - pyyaml
    - nest-asyncio
    - structlog >=24.1.0
    - click >=8
    # torch / xformers
    - pytorch >=2.4.0,<2.11
    - xformers >=0.0.27.post2
    - bitsandbytes >=0.45.5,!=0.46.0,!=0.48.0
    # triton
    - triton >=3.0.0
    # huggingface
    - wheel >=0.42.0
    - packaging
    - numpy <2.4
    - tqdm
    - psutil
    - tyro
    - protobuf
    - sentencepiece >=0.2.0
    - datasets >=3.4.1,!=4.0.*,!=4.1.0,<4.4.0
    - accelerate >=0.34.1
    - peft >=0.18.0,!=0.11.0
    - huggingface_hub >=0.34.0
    - hf-transfer
    - diffusers
    - transformers >=4.51.3,!=4.52.0,!=4.52.1,!=4.52.2,!=4.52.3,!=4.53.0,!=4.54.0,!=4.55.0,!=4.55.1,!=4.57.0,!=4.57.4,!=4.57.5,!=5.0.0,!=5.1.0,<=5.3.0
    - trl >=0.18.2,!=0.9.0,!=0.9.1,!=0.9.2,!=0.9.3,!=0.15.0,!=0.19.0,<=0.24.0
    - sentence-transformers <6
    - unsloth_zoo >=2026.9.1
    - torchvision

test:
  # importing requires a GPU
  # imports:
  #   - unsloth
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}.*

about:
  home: https://github.com/unslothai/unsloth
  summary: 2-5X faster LLM finetuning
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - isuruf
    - timkpaine
